const FOLDER_UPLOAD_IMAGES_CLOUDINARY = "api-my-album";

const MAX_FILE_SIZE = 1024 * 1024 * 2; // 2mb

const MAX_FILES_UPLOAD = 2;

module.exports = {
  FOLDER_UPLOAD_IMAGES_CLOUDINARY,
  MAX_FILE_SIZE,
  MAX_FILES_UPLOAD,
};
